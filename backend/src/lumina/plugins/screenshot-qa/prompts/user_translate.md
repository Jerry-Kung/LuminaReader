Translate the source content (the text extracted from the screenshot) into ${target_lang}. Output only the translation in the <answer> block, with no explanations, no summaries, and no commentary.

Additional instruction from the user (ignore if empty):
${user_input}
