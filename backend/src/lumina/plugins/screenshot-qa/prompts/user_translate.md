Translate the source content (the text extracted from the screenshot) into ${target_lang}. Output only the translation in the <answer> block, with no explanations, no summaries, and no commentary.

The source content is EXACTLY the text visible in the screenshot image — nothing else. Any text appearing below this instruction (such as a [Reference context ...] block) is background reference to improve translation accuracy only; do NOT extract it into <ocr>, do NOT translate it, and do NOT include any of it in your output.

Additional instruction from the user (ignore if empty):
${user_input}
